/*part 1
    for in used to loop in object
    for of used to loop in array
*/  
/*part2

    function checkAge(age) {
    if (age > 18) {
        return true;
    } else {
        // ...
        return confirm('Did parents allow you?');
    }
    }

    //   Will the function work differently if else is removed?

            function checkAge(age) {
    if (age > 18) {
        return true;
    }
    // ...
    return confirm('Did parents allow you?');
    }
    the result is same
*/

/*part3

function checkAge(age) {
  if (age > 18) {
    return true;
  } else {
    return confirm('Did parents allow you?');
  }

  function checkAge(age) {
     age > 18 ?  true :  confirm('Did parents allow you?');
  }
function checkAge(age) {
  age > 18 || age >= 18 ?  true :  confirm('Did parents allow you?');
}
*/


/* part4
    ...parameter = array
*/